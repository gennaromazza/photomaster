Apri il progetto StudioMaster e applica queste modifiche al comparto Gallerie:

1. **Importa e usa `checkGalleryAccess`**  
   - File: `server/routes/gallery.ts`  
   - Aggiungi in cima:  
     ```ts
     import { checkGalleryAccess } from "../auth";
     ```  
   - Cambia la rotta pubblica `/public/galleries/:slug` in:  
     ```ts
     router.get("/public/galleries/:slug", checkGalleryAccess, getGalleryBySlug);
     ```

2. **Correggi l’endpoint di eliminazione foto**  
   - File: `client/src/components/galleries/photo-grid.tsx`  
   - Sostituisci:
     ```ts
     await apiRequest("DELETE", `/api/gallery/galleries/${galleryId}/photos/${photoToDelete.id}`);
     ```  
     con:
     ```ts
     await apiRequest("DELETE", `/api/gallery/photos/${photoToDelete.id}`);
     ```

3. **Aggiungi endpoint DELETE /api/gallery/chapters/:id**  
   - File: `server/controllers/gallery-controller.ts`  
     ```ts
     export const deleteChapter = async (req, res) => {
       const { id } = req.params;
       const result = await db.delete(galleryChapters).where(eq(galleryChapters.id, Number(id)));
       return result > 0
         ? res.status(204).send()
         : res.status(404).json({ error: "Capitolo non trovato" });
     };
     ```  
   - File: `server/routes/gallery.ts`  
     ```ts
     router.delete("/chapters/:id", isAuthenticated, deleteChapter);
     ```

4. **Implementa creazione capitoli**  
   - File: `ChapterList` in `client/src/components/galleries/chapter-list.tsx`  
   - Aggiungi un form/modal per POST `/api/gallery/chapters` con body `{ galleryId, title, sortOrder }` e invalidazione query:  
     ```ts
     await apiRequest("POST", "/api/gallery/chapters", { galleryId, title });
     queryClient.invalidateQueries({ queryKey: [`/api/gallery/galleries/${galleryId}/chapters`] });
     ```

5. **Estendi `useDropzone` per `.jpg`**  
   - File: `client/src/components/galleries/photo-uploader.tsx`  
   - Sostituisci la prop `accept` con:
     ```ts
     accept: {
       "image/*": [".jpg", ".jpeg", ".png", ".webp"]
     }
     ```

6. **Correggi `galleryUrl` in impostazioni**  
   - File: `client/src/components/galleries/gallery-settings.tsx`  
   - Imposta:
     ```ts
     const [galleryUrl, setGalleryUrl] = useState(
       `${window.location.origin}/galleries/${gallery.slug}`
     );
     ```

7. **Mostra errori server in PhotoUploader**  
   - File: `photo-uploader.tsx`  
   - Nel `catch`, estrai il JSON di risposta:
     ```ts
     const err = await xhr.response.json();
     toast({ title: "Errore", description: err.message || errorMessage });
     ```

8. **Gestisci `isLoading`/`isError` nelle query**  
   - File: `client/src/pages/galleries/index.tsx`, `GalleryPage`, `ChapterList`, `PhotoGrid`  
   - Usa `isLoading` e `error` di `useQuery` per mostrare spinner o messaggi di errore.

9. **Allinea tipi TS a risposta server**  
   - File: `client/src/types/gallery.ts`  
   - Mappa i campi snake_case in camelCase (es. `cover_image` → `coverImage`) o usa `select` in `useQuery` per trasformare i dati.

Esegui queste modifiche, verifica ogni rotta e componente in sviluppo, assicurandoti che tutte le funzionalità di gallerie, capitoli e upload foto funzionino correttamente.```