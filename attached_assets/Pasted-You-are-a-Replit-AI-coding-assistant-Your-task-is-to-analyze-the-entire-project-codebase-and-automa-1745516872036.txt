You are a Replit AI coding assistant. Your task is to analyze the entire project codebase and automatically refactor it to fix all inconsistencies in the “Collaboratori” module, both client-side and server-side, as well as database schema and migrations.

Steps:
1. **Scansione**  
   - Analizza tutte le cartelle `client/`, `server/`, `shared/` e `migrations/`.
   - Identifica file, directory, rotte e nomi di tabelle in italiano (“collaboratori”, “eventi_collaboratori”, “pagamenti_collaboratori”) e in inglese (“collaborators”, “event_collaborators”, “eventCollaborators”).
2. **Unificazione del naming**  
   - Scegli la lingua inglese per file, cartelle, componenti, router e tabelle.
   - Rinomina file e cartelle `collaboratori/` → `collaborators/`, `schema-collaboratori.ts` → `schema-collaborators.ts`, ecc.
3. **Refactoring degli import**  
   - Aggiorna tutti gli `import` e i percorsi in front-end (`React`, `React-Query`) e back-end (`Express`, ORM) per riflettere i nuovi nomi.
4. **Rotte e API**  
   - Unifica le rotte `/api/collaboratori` e `/api/collaborators` in un unico router con path `/api/collaborators`.
   - Aggiorna controller, middleware e chiamate client per usare sempre `/api/collaborators`.
5. **Schema e migrazioni DB**  
   - Consolidare in un solo modulo Drizzle-ORM lo schema delle tabelle `collaborators`, `event_collaborators`, `payments_collaborators`, `attachments`, eliminando duplicati in italiano.
   - Aggiorna le migrazioni SQL per rinominare tabelle e colonne (`collaboratore_id` → `collaborator_id`).
6. **Pulizia**  
   - Rimuovi file, directory e moduli duplicati non più utilizzati (es. i vecchi `schema-collaboratori.ts` e `manual_collaborators_module.sql`).
7. **Test & Build**  
   - Esegui la build del front-end e del back-end, poi esegui i test automatici per verificare che tutto funzioni con il nuovo naming.
8. **Report**  
   - Alla fine, genera un breve report delle modifiche applicate (file rinominati, rotte aggiornate, tabelle migrate).

Make sure all imports, percorsi e dipendenze siano coerenti, ed esegui eventuali correzioni di conflitti nel codice per garantire che l’applicazione compili e funzioni correttamente.```
